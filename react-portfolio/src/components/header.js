import React from 'react';
import NavBar from "../components/navbar";

const Header = (props) => {
    return (
        <header> 
            <div>

            </div>
            <NavBar /> 
        </header>
    )
}

export default Header;